<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ragproject.ragserver.mapper.ChatMessageMapper">

    <resultMap id="ChatMessageResultMap" type="com.ragproject.ragserver.model.ChatMessage">
        <id property="messageId" column="message_id"/>
        <result property="sessionId" column="session_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="tokenCount" column="token_count"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.ragproject.ragserver.model.ChatMessage" useGeneratedKeys="true" keyProperty="messageId">
        INSERT INTO t_chat_message (session_id, role, content, token_count)
        VALUES (#{sessionId}, #{role}, #{content}, #{tokenCount})
    </insert>

    <select id="findBySessionId" parameterType="long" resultMap="ChatMessageResultMap">
        SELECT message_id, session_id, role, content, token_count, created_at
        FROM t_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY message_id ASC
    </select>

</mapper>
