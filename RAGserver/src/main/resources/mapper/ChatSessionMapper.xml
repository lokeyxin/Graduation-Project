<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ragproject.ragserver.mapper.ChatSessionMapper">

    <resultMap id="ChatSessionResultMap" type="com.ragproject.ragserver.model.ChatSession">
        <id property="sessionId" column="session_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.ragproject.ragserver.model.ChatSession" useGeneratedKeys="true" keyProperty="sessionId">
        INSERT INTO t_chat_session (user_id, title, status)
        VALUES (#{userId}, #{title}, #{status})
    </insert>

    <select id="findByUserId" parameterType="long" resultMap="ChatSessionResultMap">
        SELECT session_id, user_id, title, status, created_at, updated_at
        FROM t_chat_session
        WHERE user_id = #{userId}
          AND status = 1
        ORDER BY updated_at DESC, created_at DESC
    </select>

    <select id="findBySessionIdAndUserId" resultMap="ChatSessionResultMap">
        SELECT session_id, user_id, title, status, created_at, updated_at
        FROM t_chat_session
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
          AND status = 1
        LIMIT 1
    </select>

</mapper>
